/**
 * StudentController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `StudentController.Campus()`
   */
  Campus: async function (req, res) {
    return res.json({
      todo: 'Campus() is not implemented yet!'
    });
  },

  /**
   * `StudentController.Car()`
   */
  Car: async function (req, res) {
    return res.json({
      todo: 'Car() is not implemented yet!'
    });
  },

  /**
   * `StudentController.CarLine()`
   */
  CarLine: async function (req, res) {
    return res.json({
      todo: 'CarLine() is not implemented yet!'
    });
  }

};

